# =============================================================================
# Production requirements for the Marzouki Django e-commerce site.
# Pinned to the major version that the existing venv was developed against.
# =============================================================================

# --- Core framework -----------------------------------------------------------
Django==5.2.10

# --- Database -----------------------------------------------------------------
psycopg2-binary==2.9.11
dj-database-url==3.1.0

# --- Static / media serving ---------------------------------------------------
whitenoise==6.11.0
Pillow==12.1.0

# --- Cache --------------------------------------------------------------------
redis==7.1.0
django-redis==6.0.0

# --- Forms / admin UX ---------------------------------------------------------
django-crispy-forms==2.5
crispy-bootstrap5==2025.6
django-nested-admin==4.1.6

# --- Payments -----------------------------------------------------------------
stripe==14.1.0

# --- Process management & secrets --------------------------------------------
gunicorn==23.0.0
python-dotenv==1.2.2

# --- Rate limiting ------------------------------------------------------------
django-ratelimit==4.1.0

# --- HTTP / utilities ---------------------------------------------------------
requests==2.32.5

# =============================================================================
# Optional: error monitoring.  Uncomment if you set SENTRY_DSN.
# sentry-sdk[django]==2.18.0
# =============================================================================
